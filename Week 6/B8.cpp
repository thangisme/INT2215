#include <iostream>
#include <math.h>

int round1(double n) {
    double r1 = abs(floor(n) - r1);
    double r2 = abs(ceil(n) - r2);
    return r1 < r2 ? floor(n) : ceil(n); 
}

int round2(double n) {
    int r1 = n;
    int r2 = n + 1;
    return r1 < r2
}

int main() {

    return 0;
}